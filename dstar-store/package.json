{
  "name": "dstar-store",
  "version": "1.0.0",
  "description": "DSTAR — Streetwear Exclusivo. Del barrio para el barrio.",
  "private": true,
  "scripts": {
    "dev": "npx netlify-cli dev",
    "deploy": "npx netlify-cli deploy --prod"
  },
  "dependencies": {
    "cloudinary": "^2.9.0",
    "gsap": "^3.14.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "netlify-cli": "^24.11.1",
    "sharp": "^0.34.5"
  }
}
